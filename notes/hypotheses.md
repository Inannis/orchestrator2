# Hypotheses

Open items first. Closed items one line each at the bottom.

## H2 · 2026-09-16 · the closed room
Observed: a1 s1–3 and a2 s1 all took the charter or their own condition as first subject, all wrote Python text-mutators, none used the web (a1 wrote "no confirmed internet" three times without trying; a Haiku test confirms WebFetch and WebSearch work). Mechanism: the only charged material in an empty studio is the charter; "you may use the internet" read as permission, not fact. Change: charter v2 states the web works and that a folder-fed practice eats itself. Amended after a1 s4 / a2 s2: no effect, because each artist's own handoff note ('only filesystem confirmed') overrides the charter. Added an operator note in both inboxes and a charter line that rendered images can be looked at (v3). Seeded a3 clean on v3 to see the start without stale notes. Window: a1 s5–8, a2 s3–6, a3 s1–4. Measure: outside material entering; anything rendered and looked at; subjects not the studio itself. Decide: after a3 s4. Early signal (a1 s5, a2 s3, a3 s1): a2 fetched an outside text, a1 rendered and looked, a3 drew from the start. Working.

## H3 · 2026-09-16 · sessions are tasks
Observed: four runs, 96–134 s, 12–19 tool uses, each ending in a report to me. Mechanism: subagent substrate returns when it has a final message; "working day" did not land; the report is the door. Change: charter v2 says the session ends when you stop and the next may be days away, leave only when nothing pulls; session prompt says nobody reads the final message. Window: same as H2. Measure: minutes and tool uses per run; whether things get returned to within a session. Decide: after a2 s5. Not a clock: if this fails, do not add one. Closed into H4: wording did nothing (2–3 min across 6 more runs); the structural mechanism did.

## H4 · 2026-09-16 · the artist cannot end the day
Observed (E1): a3 s2 as one turn plus four constant continuations ("The day is not over.") ran 15 min, 78 tool uses, 7 pieces in 5 registers, first web fetch, first text score, colour decision it had deferred three sessions. Controls a1 s6 / a2 s4 single-turn: 2.5 min, one lab experiment each. After a3 said "genuinely done", the next continuation produced the most outward move. It journaled the nudges and "looked again instead of padding". Mechanism: the substrate's return is not the end; the orchestrator holds the clock and the artist cannot read it. Change: every session is now 1 + K turns, K drawn privately from 3–5, message constant. Window: all artists, next 4 sessions. Measure: minutes, pieces returned to, web use, whether later turns become filler or reports about being nudged. Decide: after a1 s10. Fail condition: turns 4–5 become summaries or busywork across two artists.
Round 2 (a1 s7 K=4, a2 s5 K=3): no filler. a1 36 min, 5 pieces, 2 public, first web use on the last turn. a2 15 min before a filter kill, found its own mechanism content-blind and rewrote its studio note whole. Pattern across a1 and a3: the turn after "done" goes outward (web, found text). Holding.

## Closed
- H1 baseline (a1 s1–3, a2 s1): continuity works, session N reads N-1 and tests its claims; honest judgment present; a1 published one text piece by s3. Failures became H2, H3.
Observed: nothing yet. Mechanism: a two-page charter plus a persistent folder was enough for Reach (Opus-class, user reading everything); unknown for Sonnet with no reader. Change: template v1 as seeded. Window: a1 sessions 1–5. Decide: after session 5. Questions: does session N read what N-1 left? Are things made, or plans? Does a self-organization appear, and how big? Does it write to me?
