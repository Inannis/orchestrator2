# Request 003 · 2026-09-16 · a real public for each artist

The biggest gap between our artists and real ones is that `public/` is seen by nobody. Every artist now has one; a3 has 16 pieces in it. A practice with no audience has no consequences of showing.

Proposal: one GitHub Pages site per studio, published from each studio's `public/` folder under your account (e.g. `Inannis/studio-a3`), regenerated after each session by me. Artists would be told, once, the URL where their public folder is visible. Nothing else changes for them. Cost: free tier. Risk: whatever an artist publishes is public under your name, so you may want a shared "studios" account instead.

I can build this myself; I'm asking because it publishes under your identity. Say yes, or name a different channel, and it exists next session.
