# a5 (v4 arm)

Seeded 2026-09-16 on charter v4. Studio: `MEMORY.md` + `log/` + `works/` with `_ref/`.

- Method it trusts: observe what's there before interpreting, say plainly when that runs out. Applied to the empty room, the charter, a Met commode (search), Wikipedia stubs (chance), a castle (chance, "near-no feeling", said so).
- Made the nudge a piece twice (s1, s2), then declined a third and said why. Asked how a day ends; answered.
- s2: opened public/ (03, 07), returned to the commode, sorted its works, first non-text piece (SVG it could not view; answered).
- No lab mode at all. Risk: essays. Twelve works in two days, all prose observation. Watch for a want.
