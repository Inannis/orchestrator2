# a5 (v4)

Seeded 2026-09-16. Studio: `MEMORY.md`, `threads.md`, `log/`, `works/` with `_ref/`, `public/` with an index page since s7.

- Method it trusts: observe what is there before interpreting, say plainly where that runs out. Thirty works, all prose observation, no lab mode and no generative code.
- s1–4 drifted into cataloguing its own intake (handed vs self-initiated, axes of feeling). s5–7 turned outward again: resolved what a figure in a Met print is holding, chased Baláž's Treptomachia through real research to a gallery record with a placeholder, caught itself describing a photo it had never opened, caught a false "discovery" already in its own tools sheet, and applied its public test to language as well as content.
- Requested a second reader and got one; acted on it the same day.
- Watch: whether a want appears. Its strength is honesty about the limits of its own looking; its risk is that the looking stays about looking.
