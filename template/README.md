# Template (v3, 2026-09-16)

`studio/` is copied verbatim to `artists/<id>/` to seed a new artist. `SESSION-PROMPT.md` is the only prompt an artist ever receives, with `{STUDIO}`, `{DATE}`, `{N}` filled in. Both are orchestrator-owned. Change them only as a recorded hypothesis (see `self-organization.md`).
